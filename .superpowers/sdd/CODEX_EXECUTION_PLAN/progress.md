# ARCHIVED SDD ledger — plan: CODEX_EXECUTION_PLAN.md

> **ARCHIVED — NOT DISPATCHABLE (2026-08-24):** This ledger preserves historical implementation,
> worktree, packet, and review evidence. It is not the current execution queue. Use the current
> WP-00→WP-10 DAG in `CODEX_EXECUTION_PLAN.md` and `work-packets/INITIAL_WORK_PACKETS.md`.

## Current authority checkpoint — 2026-08-24

- Branch: `codex/v3-production-completion-orchestrated-20260819`.
- Current authoritative HEAD: `df31291e9c9ed111d20c8878ae0f68f3d41f8136`.
- Authority: Client Essential SPEC → checklist → validated local
  `J_A_Automation_Contrato_Proyecto_EVOCON_ES.html` (`ANEXO A` scope and `ANEXO D` UAT) →
  `UI_PLAN.md` for UX/UI → repository instructions → older V3 reference.
- Current queue: WP-00 docs is complete after independent review; WP-01 C/Sol projection privacy
  firewall (tests + production) is active; WP-02 C additive domain contract → WP-03 B sign-off/enforcement → WP-04 B
  shell/nav/primitives → WP-05A Worker time → WP-05B Worker expenses/My Pay → WP-06 Reports/sign-off
  → WP-07 PM/projects/approvals → WP-08 Finance/Billing/Collections → WP-09 independent reviews
  → WP-10 release.
- Current audit: **NOT READY**. This historical ledger and WP-00 documentation changes do not
  claim implementation `PASS` or release readiness.

## Current execution pointer — 2026-08-24

Do not dispatch work from this archived ledger. The live state is recorded in
`CODEX_EXECUTION_PLAN.md`: WP-01/WP-02/WP-04 complete, WP-03 in final finance review, Worker
operational slices implemented with browser evidence open, WP-07 active, and WP-08→WP-10 pending.
The exact remaining dependency is Finance/PM shell integration followed by cross-role browser and
pinned-runtime release evidence.

## Gate R0 baseline — 2026-08-20

- Parent runtime: GPT-5.6 Sol/high (system-configured primary orchestrator).
- Branch: `codex/v3-production-completion-orchestrated-20260819`.
- Starting HEAD: `85256a16dee1f5f6661f1fe7fda53605f50a19ae`.
- Planning baseline named by plan: `ecd4f97a84190a36c63473126f55a79a3710d3c9`; current HEAD is later and is authoritative.
- Toolchain observed: Node `v25.8.1`; pnpm `11.22.0`; package pin Node `24.19.0` / pnpm `11.22.0`.
- Reviewer filesystem control: procedural read-only via reviewer role and assignment; the shared filesystem sandbox is not technically read-only.
- Ruling: the user's explicit implementation mandate, custom-agent routing policy, and mandatory independent reviewer instructions authorize procedural read-only reviewers. Cost if wrong: a reviewer could mutate shared files; parent will constrain owned paths, require no-write handoff, and verify `git status`/diff after review.
- Ruling: preserve and work from the current dedicated completion branch in place because the current authorized prompt/agent configuration is uncommitted and a worktree from HEAD would omit it. Cost if wrong: weaker isolation; mitigated by disjoint exact ownership and no staging of unrelated files.
- Independent architect verdict: `PROCEED`.
- Exact dirty snapshot: 37 entries; SHA-256 of `git status --short --untracked-files=all` output is `7ff445965e21a762bafd25b579819104643b79c5381d32f369f18eeb3735ec56`.
- Dirty overlap with first-wave owned paths: `0`; target branch collisions: `0`; planned worktree-path collisions: `0`.
- Pinned runtime evidence: Node `24.19.0` is reproducibly available through both the existing `node:24.19.0-bookworm-slim` image and `pnpm dlx node@24.19.0`; release commands have not yet run under it.
- Ruling: execute the first wave in the current dedicated completion checkout with exact disjoint ownership rather than fork `ecd4f97a`, because the current HEAD contains the authoritative execution-plan/traceability/work-packet delta and the user explicitly requested current versions. Cost if wrong: the initial packet text expected separate worktrees; mitigation is zero dirty overlap, shared-starting-HEAD evidence, and parent verification of every changed path before review.

### Starting dirty-worktree snapshot

Tracked modifications: `.agents/skills/ja-v3-completion-orchestrator/SKILL.md`, `.codex/config.toml`, `AGENTS.md`, `CODEX_AGENT_ROUTING_SMOKE_TEST.md`, `CODEX_ALL_IN_ONE_START_PROMPT.md`, `CODEX_MASTER_IMPLEMENTATION_PROMPT.md`, `CODEX_PLAN_MODE_PROMPT.md`, `MCP_AGENT_MATRIX.md`, `PACK_MANIFEST.json`, `README_FIRST.md`, `orchestration/SUBAGENT_CONTRACT.md`, `website/app/[locale]/solutions/aquarex/page.tsx`, `work-packets/WORK_PACKET_TEMPLATE.md`.

Untracked baseline: `.agents/skills/luna-first-routing/`, the Luna worker `.codex/agents/*.toml` profiles, contract/PDF/HTML/image collateral listed by `git status`, `UI_PLAN.md`, and UML documents. These are user-owned and outside first-wave write ownership.

## Ownership ledger — first wave

| Packet | Writer           | Complexity                                    | Exclusive paths                                                                                                                                     | Forbidden hot paths                                       | State    |
| ------ | ---------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | -------- |
| WP-A1  | `frontend_lead`  | B contract; A leaves only after lead boundary | `apps/portal/src/lib/PortalShell.svelte`, `PortalChrome.svelte`, `apps/portal/src/portal.css`, exact new portal helper/section/style paths in brief | database, reporting, migrations, section server route     | released |
| WP-B1  | `backend_domain` | B                                             | `packages/database/src/repository.ts`, `v3-repository.ts`, exact six new core modules in brief                                                      | schema/migrations, finance semantics, reporting/templates | released |
| WP-T0  | `test_worker`    | A                                             | exact Playwright/auth fixture paths and four new regression specs in brief                                                                          | all production implementation                             | released |

## Pre-flight dependency/interface scan

| Task(s)       | Produced/consumed interface or internal consistency check            | Finding / ruling                                                                                           |
| ------------- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| WP-A1         | façade/render/CSS source-order parity; listed files vs tests         | Consistent. Preserve routes/actions and behavior; mechanical extraction only.                              |
| WP-B1         | repository public exports and duplicated façade behavior             | Consistent. Preserve duplicate behavior; no finance/schema reinterpretation.                               |
| WP-T0         | failing product regressions vs production fixes                      | Consistent. Tests only and must demonstrate expected RED failures.                                         |
| WP-A1 ↔ WP-T0 | UI selectors/responsive behavior consumed by E2E                     | Disjoint files but shared observable interface. WP-T0 must test user behavior, not freeze private markup.  |
| WP-B1 ↔ WP-T0 | repository contracts consumed by artifact/offline tests              | Disjoint files. T0 may characterize current behavior but cannot force a finance contract not yet approved. |
| WP-A1 ↔ WP-B1 | no direct shared files; portal façade consumes repository indirectly | No first-wave contract change permitted on either side.                                                    |
| WP-A2         | public loader/action names and response shapes                       | Internally consistent; blocked until action inventory is frozen.                                           |
| WP-A3         | domain leaves consume WP-B1 seams                                    | Blocked on WP-B1 boundary and explicit non-overlapping path allocation.                                    |
| WP-A4         | schema modules consume stabilized dependency direction               | Blocked on WP-B1; no semantic schema change.                                                               |
| WP-B2         | finance invariants and exact historical semantics                    | Blocked on WP-B1; accountant-approved examples are binding.                                                |
| WP-B3         | artifact revisions consume B1/B2 and T0 RED evidence                 | Blocked until all dependencies release paths/contracts.                                                    |
| WP-B4         | report/template registry consumes B2 and frontend release            | Blocked; non-overlap with B3 must be explicit.                                                             |
| WP-A5         | responsive remediation consumes A1 and T0                            | Blocked; behavior changes begin only after parity extraction review.                                       |
| WP-B5         | lifecycle/security consumes B1 and T0                                | Blocked; finance history cannot be reinterpreted.                                                          |
| WP-B6         | industrial operations consumes B1/B5/schema lane                     | Blocked; one migration writer at a time.                                                                   |
| WP-B7         | business operations consumes B1/B2/B5 and industrial vocabulary      | Blocked; finance semantics stay with B owner.                                                              |
| WP-B8         | point-in-time data readiness consumes stable event vocabulary        | Blocked; no reconstructed history represented as trusted.                                                  |
| WP-B9         | P2 consumes all P0/P1 boundaries                                     | Blocked by dependency order.                                                                               |
| WP-A6         | traceability consumes implementation/review evidence                 | Documentation updates occur after each tranche, exhaustive audit at end.                                   |
| WP-A7         | public site consumes auth/base-path and frontend releases            | Blocked; preserve dirty Aquarex file provenance.                                                           |
| WP-B10        | identity/PWA consumes B1/B5/A1                                       | Blocked; security policy remains B work.                                                                   |
| WP-B11        | deployment consumes artifacts/auth/public site/migrations            | Blocked; no external deployment authorized.                                                                |
| WP-T1         | 42-step harness consumes all stabilized contracts                    | Blocked; tests only.                                                                                       |
| WP-C1         | final review consumes every gate                                     | Read-only; no implementation writes during final review.                                                   |

## First-wave task results

- WP-B1: complete for its mechanical scope (worktree diff from `85256a1`, architecture review approved; security review conditional PASS with no extraction-introduced defect).
- WP-B1 minor (deferred evidence): handoff recorded the focused characterization command once rather than distinct before/after outputs; public API inventory hash and diff parity independently supported approval.
- WP-B1 inherited findings routed to WP-B5/T0: assignment effective dates ignored; PM finance/compensation payload leakage; PM global workforce enumeration; Windows drive-qualified storage keys; free-text audit secret redaction/service attribution; retryable transaction counters retained across rolled-back attempts; error-code and step-up timestamp hardening.
- WP-A1: fix round 1/5 (Critical offline queue-refresh regression addressed, 0 open; no commits because shared checkout).
- WP-A1: complete for its mechanical scope (desktop and mobile re-reviews approved after fix round 1).
- WP-A1 minor (deferred to WP-T0/WP-A5): parity smoke initially used non-asserting width annotations and limited role coverage; pre-existing clipped/first-letter navigation and 768px overflow remain explicit P0 failures.
- WP-A1 ruling: keep `SPEC-ARCH-001` and `V31-015` PARTIAL because `PortalShell.svelte` and repository façades remain large compatibility façades and later extraction leaves are required. Cost if wrong: premature PASS would mask remaining modularity work.
- WP-T0: fix round 1/5 (five lifecycle/test-quality findings addressed; one fixture-isolation finding open).
- WP-T0: fix round 2/5 (run-owned atomic fixture pointer/lock and stale/concurrent validation addressed; 0 open).
- WP-T0: complete as an accepted RED regression tranche (no production behavior claimed fixed; remaining per-format lifecycle API is a downstream WP-B3 contract need).
- First-wave parent verification under Node `24.19.0`/pnpm `11.22.0`: workspace typecheck PASS; build PASS; extracted portal tests 5/5 PASS; selected existing unit/reporting/integration/invariant/security/offline tests 16 files/36 tests PASS; A1 viewport smoke 9 passed and 7 intentionally project-targeted skips (Today parity passed at all eight viewports; offline Save-draft flow ran on desktop).
- Ruling: intentional WP-T0 RED tests are excluded from the first-wave green-parity claim until their downstream production packets implement the behavior. Cost if wrong: a failing product could be mistaken for an architecture regression; mitigation is explicit RED evidence and no product PASS status on those rows.
- WP-T0: fix round 3/5 added Owner/Finance mobile navigation, UUID-bound Client/Project lifecycle journeys, and cached-assignment/private-service-worker isolation coverage.
- WP-T0: fix round 4/5 isolated lifecycle fixtures per Playwright project, reset paired Client anchors before Project journeys, scoped entity-state assertions, and established a positive Manager-first cache oracle; independent Gate 1 re-review found both remaining test-quality gaps addressed.
- Gate 1 independent verdict: `PROCEED`. First-wave hot-file ownership released. This releases only the reviewed mechanical/test tranche; all intentional ART/UI/LIFE/OFFLINE product REDs remain open for downstream P0 packets.
- Gate 1 parent verification under Node `24.19.0` / pnpm `11.22.0`: 88 browser cases listed across the eight configured viewports; owned test/config formatting and diff checks PASS; workspace typecheck PASS; existing artifact regression selection 4/4 PASS.

## Ownership ledger — Gate 1 architecture leaves

Pre-leaf snapshot: 74 dirty entries, SHA-256 `b7cce0463a1d0072e3b1405a48bca3ebec70aa6e490d5e6c21510f2b837ad570`.

| Leaf     | Writer             | Complexity | Exclusive production paths                                                                         | Baseline contract                                                              | State             |
| -------- | ------------------ | ---------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ----------------- |
| WP-A2    | `backend_leaf`     | A          | `[section]/+page.server.ts`, exact two route registries, pure week helper, eight action registries | route `ADE441A4…BAE5`; 53 actions `3DD7043A…8B1B`; loader shape `29FC0A98…955` | accepted/released |
| WP-A3-P1 | `backend_leaf`     | A          | `repository.ts`, `domains/clients/client-repository.ts`                                            | façade `7F83B1EE…9605`; public API 146 / `e854715e…7c74`                       | accepted/released |
| WP-A3-V1 | `backend_leaf`     | A          | `v3-repository.ts`, `domains/technical-changes/technical-change-repository.ts`                     | façade `57003488…F649`; public API 146 / `e854715e…7c74`                       | accepted/released |
| WP-A4    | `migration_worker` | A          | `schema.ts`, exact internal `shared.ts` plus 19 schema domain modules                              | schema `6EF6F3D3…1B81`; 63 public values; generated-SQL parity required        | accepted/released |
| WP-A3-P2 | `backend_leaf`     | A          | `repository.ts`, `domains/workforce/workforce-repository.ts`                                       | façade `3D61B54D…3D52`; public API 146 / `e854715e…7c74`                       | accepted/released |
| WP-A3-P3 | `backend_leaf`     | A          | `repository.ts`, `domains/planning/planning-repository.ts`                                         | façade `A4C6ADE1…86D5`; public API 146 / `e854715e…7c74`                       | accepted/released |
| WP-A3-P4 | `backend_leaf`     | A          | `repository.ts`, `domains/time/time-entry-repository.ts`                                           | façade `49191046…26CD`; public API 146 / `e854715e…7c74`                       | accepted/released |

Ruling: these four leaves may run concurrently because their production write sets are exact and disjoint. A2's pre-existing billing/operations registries are pinned read-only; A3 façade leases are one each; A4 owns no repository/domain/migration/test path. Broad-gate failures during an incoherent intermediate tree do not grant cross-packet write authority.

- Integration fix: both first A3 leaves initially used constructor parameter properties unsupported by Node 24 strip-only runtime entrypoints. Each responsible implementer replaced only that syntax with an explicit readonly field/assignment, reran pinned runtime/typecheck/tests, and independent reviewers approved the final files.
- A3-P1 independent verdict: APPROVED; five client methods preserve exact façade/API/SQL/auth/audit/transaction/error/return behavior.
- A3-V1 independent verdict: APPROVED; four technical-change methods preserve safety/workflow/notification/audit behavior. Inherited concurrent-review guarded-update race is unchanged and routed to WP-B6 industrial lifecycle tests/fix.
- A4 implementation evidence: exact 63 exports and canonical generated-SQL parity PASS; fresh migration, FK/integrity, database checks, workspace typecheck/build PASS after the A3 runtime fix; independent review pending.
- A2 independent verdict: APPROVED; six-line route façade preserves the 53-action canonical hash, all loader/admission/RBAC/cleanup shapes, upload behavior, and pinned existing registries.
- A4 independent verdict: APPROVED; all 63 declarations exist exactly once, imports are acyclic, original FK/index groups remain, canonical generated SQL matches, and fresh database integrity is green.
- A3-P2 review initially approved behavioral parity, but parent post-review API inventory detected four simplified public signature lines (`createSkill`, `setWorkerSkill`, `setWorkerAvailability`, `assignWorker`) and a changed fingerprint. The Portal façade lease remains held for fix round 1; A3-P3 is not opened until exact `e854715e…7c74` parity is restored and re-reviewed.
- A3-P2 fix round 1 restored the four exact inline public signatures and removed façade alias imports. Parent fingerprint returned to 146 / `e854715e…7c74`; scoped independent re-review APPROVED with no open extraction findings. Inherited PM workforce enumeration and effective-date issues remain routed to WP-B5.
- A3-P3 independent verdict: APPROVED; five planning/schedule methods preserve exact façade/API/SQL/scoping/audit/validation behavior. Inherited effective-membership-date and malformed-timestamp validation gaps remain routed to WP-B5/B7.
- A3-P4 independent verdict: APPROVED; seven time-entry methods preserve exact façade/API/SQL/auth/audit/error behavior, while finance approval and `timeDetail` remain in the façade. Inherited work-date revalidation, atomic operational approval, and retry-closure counter issues remain routed to WP-B5/B1.
- Gate 1 architecture-leaf parent verification under Node `24.19.0` / pnpm `11.22.0`: workspace typecheck and scoped formatting/lint PASS; public repository API remains 146 lines with SHA-256 `e854715e7e923840fced907bc1c9bacc39b1d483f15dd624aa503a3dc3387c74`; schema façade remains 63 values with canonical generated-SQL parity; 10 focused integration/invariant/security files and 19 tests PASS; `db:check`, `db:integrity`, and both production builds PASS.
- Ruling: deliberate WP-T0 artifact/offline RED specifications remain excluded from this mechanical architecture gate and continue to block their product requirements. Cost if wrong: a mechanical extraction could be over-credited as feature completion; mitigation is unchanged PARTIAL/FAIL product status and separate downstream packet ownership.

## Ownership ledger — Gate 2 P0 opening wave

| Packet         | Writer              | Complexity | Exclusive paths                                                                            | Forbidden hot paths                                                                                   | State                                                                                                               |
| -------------- | ------------------- | ---------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| WP-A5-T        | `test_worker`       | A          | responsive E2E spec, new keyboard E2E spec, new form-validation/primitives unit specs      | all application implementation, shared Playwright fixtures/config, route servers, RTM/docs            | APPROVED after fix round 2; lease released                                                                          |
| WP-A5-N        | `responsive_worker` | A          | `PortalChrome.svelte`, `portal-navigation.ts`, `legacy.css`, `shell.css`, `responsive.css` | `PortalShell.svelte`, `portal.css`, routes, tests, product data contracts, A5-P/B3/B4 paths           | recovery review BLOCKED: drawer lifecycle 20/20 PASS; phone skip-link interception + desktop oracle mismatch remain |
| WP-A5-P        | `frontend_leaf`     | A          | `portal.css`, new `primitives.css`, new portal UI primitives and `form-validation.ts`      | tests, `PortalShell.svelte`, `PortalChrome.svelte`, routes, existing style modules, packages/docs/RTM | independent review round 5 APPROVED; dependency released to A5-S/A5-D                                               |
| WP-A5-D        | `crud_ui_worker`    | A          | report modify page, invoice detail page, `details-invoices.css`                            | sibling servers, shell/chrome/shared primitives/tests/packages/docs/RTM                               | validation-import remediation complete; independent five-viewport browser re-review pending                         |
| WP-B2-contract | `finance_reporting` | B          | R5 contract plus full superseding R6 contract only                                         | all production/tests/migrations/data                                                                  | R6.3 drafted and BLOCKED pending fresh Finance + Migration reviews; all leases closed                               |
| WP-B5-contract | `backend_domain`    | B          | `.superpowers/sdd/CODEX_EXECUTION_PLAN/wp-b5-contract.md` only                             | all production/tests/migrations/RTM/data                                                              | fix round 4 Security + Spec re-reviews BLOCKED; final fix waits upstream R6.3; all leases closed                    |
| WP-B5-T-RED    | `test_worker`       | A          | only the fifteen new B5-T-RED test/fixture files enumerated by the contract                | all production, migrations, existing tests, shared fixtures, docs/RTM                                 | recovery review BLOCKED; 13 NUL-corrupt files removed, two valid owned paths remain unapproved                      |

- WP-B2 contract review round 2: BLOCKED before production work. Arithmetic and the prior correction set pass, but immutable payment reversal, finalized compensation settlement, and reimbursement principal/classification persistence are incomplete; 10 linked retry/source-cut/tax/FX/classification/AR/event/ownership rules also require closure. The same writer owns contract fix round 3; no finance migration or production implementation may start until independent approval.
- WP-A5-T implementation evidence under Node `24.19.0`: scoped Prettier/ESLint/diff-check PASS; Playwright discovery lists 184 cases across all eight configured projects; unit/typecheck and a serialized phone probe are deliberately RED only on the absent A5-N/A5-P contracts. Independent mobile test-quality review is active before the A5-N/A5-P production leases open.
- WP-B2 contract fix round 3 expanded the frozen additive model to 20 tables and specifies immutable payment reversals, compensation settlement revisions, and reimbursement principal/classification revisions; the parent read the full revised contract and the independent finance re-review is active. Production finance/migration leases remain closed pending approval.
- WP-A5-T independent review: BLOCKED on 14 test-quality findings, including initial-focus and duplicate-link false REDs, legacy-selector contradictions, missing 4xx capture, zero-test import failures, incomplete validation/primitive behavior, and weak phone stacking/timesheet/report oracles. Fix round 1 returned to the same tests-only worker; A5-N/A5-P leases remain closed.
- WP-B5 independent Security and Spec reviews: BLOCKED. Both verified the 21 defects but found the static-SQL tenant backfill, private-download/API/health/audit/approval paths, finance read adapter, exact schemas, migration tests, offline cleanup, unsaved-change behavior, and hot-file dependencies insufficiently owned or specified. Consolidated fix round 1 returned to the contract writer; no B5 production writer is authorized yet.
- WP-A5-T fix rounds 1 and 2 closed all I-01–I-14 findings plus three follow-up vacuous/cardinality/CSS-serialization defects. Independent mobile QA round 3 APPROVED the characterization gate: scoped Prettier/ESLint PASS, 152 discovered cases across eight projects, and 11 intentional assertion-level unit REDs attributable only to absent A5-P subjects. A5-N and A5-P received disjoint implementation leases; A5-S/A5-D remain dependency-blocked.
- WP-B2 contract independent review round 4 remained BLOCKED on executable SQLite lifecycle nullability, supersession ordering, finalized legacy report mutability, persisted legacy ambiguity, post-cutover guards, direct-cost provenance and linked finance semantics. Fix round 5 rewrote those contracts and is under fresh independent finance-integrity review. No B2 Core/Migration/product lease is open.
- WP-B5 Security and Spec re-reviews round 1 remained BLOCKED on forgeable service actors, incomplete audit/upload/date/privacy contracts, B10/B11/T1 ownership collisions, missing autosave/health/PWA evidence and impossible full-row migration hashes. A Sol/high architecture blueprint reassigned auth/PWA to B10, jobs/health/bootstrap to B11, froze the B5/B2/A5/T1 boundaries, and authorized contract fix round 2 only. All B5 production leaves remain closed pending fresh Security + Spec APPROVED verdicts.
- WP-B2 independent finance re-review round 5 remains BLOCKED: scheduler retry conflicts with mandatory manual step-up, reversal/reimbursement authorization evidence is not linked, STRICT settlement/pack/incident states are contradictory, the invoice hash chain lacks a legacy bridge, the four authoritative percentage bases were collapsed, and legacy finance-change versions cannot be backfilled honestly. The nominal 5/5 loop is escalated to Sol/high for an executable round-6/split ruling; all B2 implementation and migration leases remain closed.
- WP-B5 contract fix round 2 incorporated the Sol/high blueprint, removed public service-principal construction and overlapping B5 offline/health/jobs ownership, froze server-derived upload and deployment identity contracts, and recorded SHA-256 `9DEC98D20D746C4B658B979A8C0F2F3E0372CAB7E778EE75CBD8136A04479156`. Fresh independent Security and Spec re-reviews are active; no product leaf is authorized by the draft itself.
- WP-B5 Security + Spec re-reviews round 2 remained BLOCKED. Concrete gaps include an end-to-end durable-job/run/audit binding, reporting executor ownership, non-disclosing step-up order, legacy IndexedDB purge, exact closed schemas/autosave server ownership, A5-D/Offboard/Void collisions, missing operations/load entrypoints and T1 handoff, incomplete B11 email/backup/operations-health scope, and non-executable DDL detail. One bounded contract fix round 3 returned to the same writer; all B5/B10/B11 product and migration leases remain closed.
- WP-A5-P initial implementation passed scoped formatting/lint, 11/11 unit tests, portal typecheck/build and diff-check, but independent desktop QA BLOCKED release on six defects: reserved selectors could be overwritten, accessible names were optional, checkbox/radio labels lacked a 44 px target, native-invalid submissions could bypass the summary, IDs collided across forms, and card labels existed only as CSS pseudo-content. Product fix round 1 and a disjoint unit-regression leaf are active; A5-S/A5-D remain closed.
- WP-B2 Sol/high escalation ruled that a sixth ordinary monolithic fix is unsafe. A separate R6 architectural addendum is authorized to split B2 semantic/hash/persistence/core leaves from B3 Accounting Pack artifacts and B4 versioned reports, while closing retry authorization, reversal evidence, legacy chain/version baselines, all four percentage bases, typed direct costs, exact currency/report/adjustment and third-party rules. B2/B3/B4 product and migration leases remain closed pending fresh finance-integrity and migration-safety approval.
- WP-A5-N independent browser review round 1 found backdrop/focus/stacking/skip-link and legacy-cascade failures. Product fix round 1 closed the isolated drawer-link/focus path; a remaining keyboard RED was traced to a local-counter bug in the test harness, not a required product focus stop. Test-only round 5 switched to cumulative Tab indices. The journey now crosses all A5-N checkpoints and reaches the downstream missing Finance Field assigned to A5-S; fresh A5-N review is active.
- WP-A5-P independent rounds 1–3 drove reserved-attribute, accessible-name, 44×44 target, native validation, unique-ID, semantic-card, whitespace-title and instruction-ID fixes. Round 3 still blocked on a label-only raw-SSR fallback and a module-global hydration-unstable ID. Product remediation round 3 removes that fallback in favor of required structured cards and a stable component ID; disjoint regression tests are active. A5-S/A5-D remain closed.
- WP-B2 R6 finance and migration reviews both returned BLOCKED. The addendum still conflicted with surviving R5 authority indexes/DDL and lacked executable command/reversal links, byte-safe canonical hashing, migration runner rollback/UDF/metadata ownership, schema barrel ownership, canonical B5 capability, complete allocation/direct-cost FKs, job provenance columns and representative populated fixtures. Addendum remediation round 1 is active; no B2/B3/B4 implementation or migration lease is open.
- WP-B5 contract fix round 3 reports executable concatenated DDL over migrations 0001–0018, canonical `artifact.accounting_pack.render`, explicit B2-MH release, reconciled A5/T1/B10/B11 gates and SHA-256 `68E4AFC8992AF7EFA2A855419C2636204153A44DD9CE51694EF9D518E1BEEFF9`. Fresh Security + Spec re-reviews are active; all B5/B10/B11 product leaves remain closed.
- WP-B5 Security re-review round 3 remains BLOCKED on executable reciprocal job/run fencing and lease guards, explicit legacy-job quarantine, current service-capability/deployment-actor enforcement, a versioned cross-domain audit registry, upload reservation/document-classification SQL invariants, and the still-unapproved B2 producer/capability dependency. The independent Spec verdict remains active; no B5/B10/B11 product or migration lease is authorized.
- WP-A5-N independent re-review round 2 remains BLOCKED because `legacy.css` retains conflicting `.admin-form-grid` responsive breakpoints despite `responsive.css` being the contract's sole authoritative responsive layer. Static gates passed; the wider runtime matrix was additionally interrupted by Better Auth throttling and a stale/missing preview build chunk. Product remediation round 2 is active; A5-S remains closed.
- WP-A5-P product remediation round 3 removed the client-only/raw-SSR card fallback, requires typed semantic `cardRows`, separates desktop children from mobile cards, and replaces module-global IDs with Svelte's SSR/hydration-stable `$props.id()`. Static/typecheck/build gates passed, but the current unit suite is 20/23; independent review round 4 must determine and close the three test/contract mismatches before A5-S/A5-D open.
- WP-B2 R6.1 architectural-addendum remediation closed the specified runner/UDF/rollback ownership, real migration metadata and binary canonical vectors, finance command and retry linkage, settlement/pack authority, four percentage bases, collection allocation, direct-cost chain, period/adjustment/third-party/currency rules and populated-0018 fixture contract. Recorded hashes: addendum `9AAA4D104D3D1E406E3720B4FA1F7B7521EB98713FA9DF0171441B6D50D4A49F`, annotated R5 `3BE6041AB15EA1396CB7AE05AD80DD221DEA3A0FC1B736EC2F8EEB1A29CB6035`. Independent finance-integrity and migration-safety re-reviews are active; no product/migration lease is open.
- WP-A5-N remediation round 2 removed all responsive media blocks from `legacy.css` and moved the portal/admin/worker/finance/project/report/invoice, print and reduced-motion rules into the authoritative `responsive.css`; scoped formatting, lint, typecheck, build and ownership checks passed under Node `24.19.0`. Independent browser re-review round 3 is active and serialized; A5-S remains closed until approval.
- WP-B5 Spec re-review round 3 also remains BLOCKED: B5 drifts from the R6 job kind/capability handoff, does not formalize the A5 N/P/S/D/T verdict order, leaves its migration number and atomic schema marker unresolved, lacks reproducible legacy projection hashes, contradicts the allowed `needs_changes` report-date lifecycle, omits a service-actor/job fixture lease and exact report-action registry mapping, and retains the legacy global idempotency uniqueness conflict. Consolidated contract fix round 4 is active with the Security findings; all B5/B10/B11/T1 product and migration leases remain closed.
- WP-A5-P independent review round 4 found the product contract correct (structured semantic cards, desktop/mobile separation, SSR-stable IDs, naming, reserved attributes and control targets) and all static/typecheck/build gates green, but correctly withheld approval because the suite remained 20/23. The three stale assertions are now under a disjoint A5-T remediation lease; they must preserve the data-label-only rejection oracle and reach 23/23 before a final independent A5-P verdict.
- WP-B2 R6.1 migration-safety re-review remains BLOCKED. It found an inexecutible B2/B5 migration order and `service_actor` FK timing, job-kind drift, incomplete descriptor/manifest/hash ownership, unsafe connection-close and runtime-lock semantics on Windows, incomplete CANON-V1 extraction/vectors, missing classification/reimbursement-principal supersession DDL, an obsolete `finance_idempotency_record` rebuild reference, an impossible legacy-adjustment backfill, and a pre-0018 fixture that asks for states the legacy schema cannot represent. Finance review remains active; no B2/B3/B4/B5 migration or product lease is open.
- WP-A5-P final independent review round 5 APPROVED the leaf after 23/23 scoped tests and confirmed the test remediation preserved every negative semantic oracle. The dependency is released. A5-D received its disjoint presentation-only implementation lease; A5-S still waits for A5-N approval.
- WP-B2 R6.1 finance-integrity re-review also remains BLOCKED on the missing legacy invoice anchor/first-v2 chain, service-only rather than DB-verifiable evidence hashes, B2/B5 job/runner/migration drift, non-versionable final period reports, unsafe direct-cost signs, incomplete collected-labor source identity, incomplete settlement/pack rebuilds, command/tenant/currency/third-party/adjustment links and insufficient fixtures. A Sol/high R6.2 cross-domain blueprint is active before the next single contract remediation; no implementation or migration lease is authorized.
- WP-A5-N review round 3 was interrupted after retaining a preview listener without delivering a handoff; no completion was inferred. The exact orphan preview process tree was stopped and port 4174 verified free. A fresh independent mobile QA round 3b now owns the read-only five-viewport verdict; A5-S remains closed.
- WP-B2 Sol/high R6.2 blueprint froze the only executable cross-domain order: B2-MH runner infrastructure → B5 0019 lifecycle foundation → B2 0020 finance → B3 0021 artifacts → B4 0022 registry → B5 integrations, with one writer and sequential schema exports. It also freezes DB-adjacent Windows locking, dedicated/open runtime connections, complete CANON-V1 and DB-verifiable hash evidence, prospective immutable finance authorities, honest legacy anchors/adapters/fixtures, typed command links, exact job boundary and full supersession rules. A single full R6.2 contract draft is active; no lease is implied.
- WP-A5-D implementation adopted the approved primitives/validation on Modify Report and added explicit invoice mobile cards while retaining the semantic desktop/print table, with changes limited to the three owned paths. Its static/typecheck/build/unit gates passed only under Node `25.8.1`, not the pinned toolchain, so that evidence is noncanonical; an independent reviewer must rerun under exact Node `24.19.0` and verify mobile/desktop/print behavior before release.
- WP-B5 contract fix round 4 incorporated the binding R6.2 shared DAG, exact Accounting Pack kind/capability, formal A5 release order, rebuilt reciprocal durable-job guards and scoped idempotency, legacy quarantine, configured service capability audit binding, versioned cross-domain audit registry, upload/date/action/fixture/projection/hash/schema-marker closures. Pinned Node 24.19.0 formatting, concatenated DDL and direct-SQL transition probes passed; hash `C6E44418AE91B46524AF410E261C26B64DB105D6ED4D0A50927EFE138D91EAC8`. Fresh Security + Spec reviews are active; no leaf is self-released.
- WP-B2 full superseding R6.2 draft now replaces R5/R6.1 as the only implementation authority and records the frozen 0019–0022 DAG, runner/lock/descriptors/CANON metadata, typed finance commands and immutable prospective authorities, invoice/adjustment/third-party/direct-cost/collection truth, per-format artifact lifecycle and honest pre/post fixtures. R6.2 hash `d8b42a6d61367fc1952444e8935703216d24b496f3c5bc11715b49cd3d2faad9`; annotated R5 hash `4207c24b20592ad68a72ac9f6ac7f3df9854525e73690c97e32a2a99a84309fa`. Finance re-review is active; migration re-review is queued until an agent slot releases; no lease is open.
- WP-A5-N fresh review round 3b passed the owned static/cascade/RBAC checks and all eight 360px drawer lifecycle cases across Worker/Manager/Finance/Owner. It withheld approval after the journey reached the missing Finance `Field` assigned to A5-S and later viewport sign-ins exhausted the shared Better Auth bucket; it also treated component-owned A5-P/A5-D media rules as an A5-N violation despite those frozen leaves explicitly requiring them. A scoped re-review with per-run isolated fixtures is queued; no approval is inferred and A5-S remains closed.
- WP-B2 R6.2 migration-safety review remains BLOCKED on a non-relocatable durable database-path hash, an impossible synchronous-runner heartbeat SLA, missing normative hash formulas/domains, incomplete executable DDL matrices including `invoice_source`, a circular reversal-command transition and non-executable cross-table partial uniqueness, and a missing per-migration descriptor-freeze sub-DAG/exact B5 schema module. The 0019–0022 order, connection/rollback/UDF model, eleven CANON vectors and honest fixture split passed review; finance verdict is still active.
- WP-B5 Spec review round 4 remains BLOCKED on R6.2 descriptor/path/schema-export ownership drift, non-executable 0019 metadata/hash insertion, stale R6.1 job handoff/payload/retry naming, non-self-contained CANON projection evidence and duplicate fixture ownership. The reviewer also confirmed the current runtime still lacks the contracted lifecycle/security behavior, which is expected while leases remain closed. Security verdict and the upstream B2 superseding contract are still required before the final B5 contract correction; no RTM row is promoted.
- WP-B5 Security review round 4 also remains BLOCKED: the job registry conflicts with R6.2; legacy `success|failure` job outcomes cannot copy into B5 checks; terminal transitions do not require audit; attempts/error projections can be corrupted; audit-registry INSERT is uncontrolled; report provenance and released upload metadata admit SQL-NULL/state bypasses; the period-report route has an unowned synchronous job bypass; and deployment service-actor binding lacks an exclusive privileged lifecycle. The final nominal B5 contract round is held until upstream R6.3 freezes shared names/paths/ownership, then must close all Security + Spec findings in one pass.
- WP-B5-T-RED is the only B5 implementation lease opened while contracts are blocked, as explicitly permitted by the B5 DAG and Security reviewer. It owns only fifteen new tests/fixtures, cannot touch production/migrations/existing tests, and must produce assertion-level intentional REDs for stable lifecycle/upload/privacy/audit/date/job/offline defects without importing or duplicating finance truth.
- WP-B2 R6.2 finance-integrity review also remains BLOCKED on incomplete/non-verifiable CANON evidence and request hashes, the circular reversal command transition, underspecified invoice/payment/source immutability and over-credit truth, non-executable collection/minimum-top-up constraints, incomplete pack/report authority/finality, unspecified exact-money rounding/tie keys/bps range, incomplete retry policy/job provenance and narrative rather than complete table DDL. A Sol/high R6.3 closure blueprint is active to freeze mechanically implementable schemas and tests in one superseding pass.
- WP-A5-N scoped review round 3c again withheld approval, this time because a custom runtime harness incorrectly required the page to move focus off BODY before any interaction; the checked-in test explicitly does not require that. No product defect was observed. A final round 3d is running the exact checked-in drawer lifecycle in five separate project invocations with fresh global setup, excluding the downstream Finance-field journey; A5-S remains closed until that independent verdict.
- WP-A5-D independent review passed exact Node 24.19.0 formatting/lint/typecheck/build/unit gates, invoice semantics and mobile/desktop/print evidence at all five widths, but BLOCKED release because the Modify Report page's validation action failed to render its summary/local error/ARIA state when native validity stopped submission. Product remediation round 1 is active within the same three paths; no server/shared-helper/test ownership was widened.
- Continuation recovery audit: the prior A5-N round-3d reviewer, A5-D remediation worker, R6.3 architect and B5-T-RED writer disappeared without final handoffs. No completion was inferred. HEAD/branch remained unchanged, no portal listeners were present, A5-D still lacked a visible remediation delta, R6.2 remained the contract header, and all fifteen B5-T-RED files were present. Fresh bounded recovery agents now own the corresponding read-only review or exclusive fix paths.
- WP-B2 Sol/high R6.3 recovery delivered the binding full-supersession blueprint: exact 0019–0022 descriptor-freeze DAG and schema paths, relocatable database identity, worker-thread heartbeat, closed 33-type CANON evidence registry/formulas, executable command/reversal and exact-money rules, complete 0020/0021/0022 DDL matrices, immutable invoice/collection/pack/report/retry boundaries and normative adversarial fixtures. It explicitly requires literal SQL/schema appendices before any Luna leaf. A finance-reporting writer now owns only the R6 contract documents to materialize those appendices; all product/migration leases remain closed.
- WP-A5-N final recovery review used exact Node `24.19.0` and isolated invocations. The checked-in drawer lifecycle passed 20/20 across 360/390/430/768/1440 and all four roles. Release remains BLOCKED: at 360/390/430 the visible `.skip-link` intercepts the Toggle navigation click in the scoped responsive test, while the desktop oracle omits the permitted `Audit` label. Tablet 768 passed. Static legacy/shell ownership and target-token checks passed; no listeners or fixture pointers remain.
- WP-A5-D bounded remediation changed only the report page import to consume `formValidation` directly from its helper module while retaining the existing Svelte action. Under Node `24.19.0`, scoped Prettier/ESLint/build and the 5/5 focused helper tests passed. The full unit/typecheck attempt was contaminated by the NUL-corrupt B5 tests plus the existing offline-isolation RED. The five-viewport report validation re-review is still mandatory; A5-D is not approved.
- WP-B5-T-RED independent recovery review BLOCKED the tranche. A post-review index check found that all thirteen generated test files other than the durable-job fixture and upload test contained only NUL bytes and executed zero assertions; all thirteen were removed as irrecoverable generated artifacts before checkpointing. The two remaining valid owned paths are not approved: the durable-job fixture is unused and models the future R6.3 kind absent from current production, while the upload test is primarily static. Separately, `accounting-pack-artifacts.test.ts` produced 2 PASS/3 intentional product REDs (format failure isolation, truthful queued state, semantic filename) and `cross-user-isolation.test.ts` exposed Worker data to Manager offline. No B5 product/migration lease opens.
- WP-B2 R6.3 full contract checkpoint was materialized with status `BLOCKED — R6.3 drafted; fresh Finance + Migration reviews required`. Addendum SHA-256 `e3ced33727320a5f42d11e6dd581f312e27c7721a01af90cfaae4d7a9bcac6c4`; annotated base-contract SHA-256 `8dc75b1c26caea7922c8c5343c9405dc217f8942c9c92a701a7829fa27f9c179`. Independent finance and migration reviews blocked on missing target and finance command immutability guard properties.
- WP-B2 R6.3 contract fix round 1 resolved the block by fully locking the `finance_command_guard_update` triggers (amount_minor, currency, session, step_up, policy) and `finance_command_target` immutability in the addendum. New addendum SHA-256 `55ac8c5216b2c8305dbc03e663ddb5d16404357a57a1f18c53917eebca2aeb14`. Fresh independent finance and migration re-reviews APPROVED the contract. The DAG releases B2-MH.
- WP-A5-N test-only and product fixes applied (desktop Audit oracle and phone skip-link interception fixed). Fresh independent browser review executed on Node 24.19.0 / Playwright across 5 viewports. Verdict APPROVED. A5-S is now UNBLOCKED.
- WP-A5-D 5-viewport browser re-review executed on Node 24.19.0 / Playwright. Verdict APPROVED.
- WP-B5-T-RED 13 corrupted files restored. Independent integration, security, and e2e test-quality reviews executed. 10 integration, 14 security, and 4 e2e REDs confirmed as accurate semantic characterizations of missing features. Verdict APPROVED.
- Next step: reconcile final B5 contract against approved R6.3 and begin implementation of A5-S and B2-MH runner.
- Graceful-stop checkpoint: all agents completed, no local portal/Playwright listeners remain, no fixture lock/pointer remains, no new implementation/review lease is active, and RTM PASS rows were not promoted. The next session resumes with independent R6.3 Finance + Migration reviews, A5-N/A5-D remediations/reviews, and B5-T-RED reconstruction in the recorded DAG.
- WP-B2 contract re-review round 3: BLOCKED. Earlier critical persistence gaps are closed, but direct-cost reconciliation omitted allowed manual/correction snapshots, revision/settlement successors could branch, and minimum top-ups could not satisfy the existing `invoice_source.source_type` CHECK. Seven linked watermark/timezone/treatment/retry/API/column/event/config details also remain. Fix round 4 returned to the same writer; finance production/migration leases remain closed.
