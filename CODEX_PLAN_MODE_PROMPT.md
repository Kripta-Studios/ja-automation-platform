# Client Essential audit and execution-queue prompt

Read `AGENTS.md` and its mandatory Client Essential authority completely. Inspect the current branch, dirty worktree, code, tests, migrations, deployment, and runtime evidence before proposing changes.

Act as the Sol lead orchestrator. Product-scope interpretation stays with the parent. Use bounded read-only agents to gather evidence, then classify each CORE-01..17 bullet and the Client Essential DoD as `PASS`, `PARTIAL`, `FAIL`, `BLOCKED`, `CONDITIONAL`, or `DEFERRED`. Never infer `PASS` from code presence or stale Markdown.

Produce a dependency DAG from remaining Essential `PARTIAL`/`FAIL` items. For every implementation packet define requirement IDs, preconditions, A/B/C complexity, assigned profile, owned/forbidden paths, contracts, required behavior, tests, acceptance criteria, independent reviewer, and handoff. Prevent overlapping write ownership.

Retarget stale instruction/orchestration documents so V3.1–V3.4 expansion, all P0/P1/P2 items, the 207-row RTM, and the old 42-step scenario remain visible as deferred roadmap but cannot block Client Essential delivery. Preserve production security, finance, lifecycle, migration, deployment, and recovery invariants.

Update `CODEX_EXECUTION_PLAN.md` and the Client Essential checklist with honest current evidence. Then immediately begin the first unblocked packet; planning is Wave 0, not the deliverable.
