# Next actions — Client Essential candidate 2026-09-06

## Completed

1. Exact application SHA `2058db24ca4d5d6b3f66bde11b6230e271a2d06c` is implemented,
   independently reviewed (`ship`), tested and deployed.
2. The 32-step executable UAT passes 32/32 with synthetic authenticated Owner/Worker/PM/Finance
   accounts and fresh production operations/Caddy evidence.
3. Two new automatic jobs cycles passed with zero failures; local backup and rollback are retained.
4. Caddy validates; EN/ES/PT and portal login return 200 over verified TLS; internal readiness remains
   hidden from the public boundary.
5. MFA is optional and no operation uses step-up authentication. The Owner's decision controls this
   delivery despite older contradictory text.
6. EN/ES/PT-BR employee manuals and the 126-output artifact matrix are generated, hash-bound to the
   frozen application SHA and fully reader/visual checked.
7. Designated production Owner/Worker authenticated smoke passed without MFA: Owner protected routes
   work; Worker confidential finance routes are absent from navigation and enforced as 403, with an
   unassigned-project probe returning 404.
8. Authorized TEST/SYNTHETIC workflow cleanup is audited: zero active time/expense/report/assignment,
   two archived projects/clients and two intentionally immutable committed synthetic receipts.
9. The literal live Worker path passes through project → time → expense/private receipt → daily report
   → expected-payment view; the Owner's canonical Finance route exposes planned/budget metrics, while
   Worker serialization contains no confidential budget, internal-cost or client-rate keys.
10. Live website and login pages return the correct EN/ES/PT-BR document language and localized title.

## Owner/human actions still required

1. Tax adviser/Accounting supplies the verified EIN/TIN, confirms the tax/legal address, approves
   separate Labor and Expenses tax profiles, and confirms the due-date rule. Supplied issuer,
   currency override, rounding, configurable series, secure remittance and invoice-RBAC decisions are
   already recorded.
2. Legal/privacy owner confirms the discovered Hetzner/Falkenstein (Germany) hosting inventory,
   self-hosted Stalwart classification, authorized US/BR access/transfers and any off-code provider;
   then expressly approves the DPA wording and retention/deletion schedule. The current answer is
   `NO / PENDING`; proposed periods are not legal determinations and irreversible deletion remains
   disabled.
3. Record actual human acceptance by the Owner for Accounting outputs, the Worker for the manual
   walkthrough, marketing/legal for EN/ES/PT content, and formally identify authorized signers.
4. Security owner decides whether historical credential exposure requires a controlled live rotation.
5. Accounting and content approvers review the frozen workbook/manual/site outputs and sign the
   acceptance record. No automated test substitutes for their signatures.

No push, PR, DNS change, account/password/email or mail-server mutation, real invoice send or payment
record is authorized by this file. The truthful state remains `TECHNICALLY_VERIFIED_AWAITING_OWNER`
until the actions above are completed.
