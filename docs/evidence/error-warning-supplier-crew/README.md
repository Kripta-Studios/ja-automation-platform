# Crew delegation browser evidence — 2026-09-25

These files were regenerated by a Playwright Chromium run against a disposable database, using an isolated copy at `/tmp/ja-qa-late-20260925` with the current uncommitted crew and closeout changes. The copy was built with `pnpm --filter @ja/portal build`, served at `127.0.0.1:4186`, and tested at 390 px and 1440 px. The associated site used port 4185. No production account or data was used.

Command: `pnpm exec playwright test tests/e2e/error-warning-finance-billing.spec.ts tests/e2e/error-warning-supplier-crew.spec.ts --project=phone-390 --project=desktop --grep 'closeout exact review and recovery|crew delegation explains' --reporter=line` (isolated config uses ports 4185/4186). Result: **4 passed, 2 intentionally skipped** in 59 seconds; the two crew cases passed.

The crew run used actual browser controls and network responses. It checked same-person and inactive-assignment errors, successful delegation, a duplicate native form POST returning 409, retained project/date/language and form values, keyboard focus, and manager-role denial. The trace files contain the scroll coordinates before and after the duplicate action: phone 479 → 479 px, desktop 0 → 0 px. The network JSON contains only HTTP status and path. Screenshots show the problem notice; the fixture contains synthetic QA data.

The same invocation also passed the closeout phone and desktop cases; their artifacts are in `docs/evidence/error-warning-finance-billing/closeout-*`. This folder documents only crew delegation.
