# Deployed crew, expense and report journey

2026-09-24, public origin `https://j-aautomation.com`, portal/jobs image `ja-automation-portal:zip-b0276b4dbd5583c3315794d91cbca213`. Ordinary authenticated chief and owner browser sessions were used. The test source is `tests/production/crew-expense-report.acceptance.spec.ts`; it is guarded by an explicit QA project ID and runs writes only once in the desktop browser. It does not issue invoices, record payments, or send mail.

The deployed Playwright run passed **2/2** in 11.9 seconds. The chief opened the designated QA project at `/j-aautomation/app/crew?project=01a0cfae-9005-76a9-871a-8cf4edf877dd&date=2026-09-24` with HTTP 200 and two delegated workers. The chief saved a shared 1.25 hours for each, then individual 0.5 and 0.75 hours, and reloaded each result. The four uniquely marked time rows remained visible. A shift's **Add expense** link carried the exact project, worker, date and time-entry IDs to the Expenses form. The chief saved a €1.25 draft parking expense at 14:35, then its detail and linked time record survived reload. The chief detail did not expose worker pay, internal cost, client rate or billing treatment. The owner followed Project → Time, Project → Reports, and Time → specific daily report, including report reload.

Read-only verification of the live SQLite rows for marker `QA CREW LIVE 1790249453054` found four time rows: External Technician Test 75 and 30 minutes, Worker Test 75 and 45 minutes. All have null start/end clocks, `draft` approval state, and `recorded_by_user_id=93661009-f7f8-4e9d-a3fa-3897c52e02e2` (Supplier Coordinator Test). The linked expense `01a0d32e-d320-767c-bb7b-62fc2e251cfc` belongs to External Technician Test, the QA project and 2026-09-24; it records `occurred_time_local=14:35`, `amount_minor=125`, `time_entry_id=01a0d32e-c953-7311-961a-bbdf73e53187`, and the same chief actor. The time rows and expense are marked QA drafts for the planned clean-slate removal. This read used SQLite `mode=ro` and made no database mutation.

The same browser journey uploaded one clearly marked QA receipt, saved a €3.00 draft at 15:05 linked to External Technician Test's shift, then allocated €2.00 to that shift and €1.00 to Worker Test's shift. The crew UI showed **€3.00 · one expense** and both allocations after reload. A read-only SQLite check for marker `QA CREW LIVE 1790249555948` found one receipt document, one expense of 300 minor units, and exactly two allocation rows totaling 300 minor units. The four corresponding time rows persisted as 75/75/30/45 minutes with null intervals and the Supplier Coordinator Test as actor. Both expenses have that same actor. This prevents an apparent two-person share from multiplying the financial expense.

This result proves browser saves, reloads, per-person/shared-hour modes, chief actor attribution, linked expenses with local occurrence times, a two-worker shared receipt allocation, and the project-to-report path on this release. It does not prove invoice issuance in production; that requires separate evidence.

A separate read-only WebKit run passed **2/2** on 390-px iPhone and 768-px iPad profiles. The authenticated chief loaded the real crew page, switched from shared hours to per-worker inputs, and could reach a 44-px-high save control with no horizontal document overflow. These are browser-device emulations, not physical-device tests.
