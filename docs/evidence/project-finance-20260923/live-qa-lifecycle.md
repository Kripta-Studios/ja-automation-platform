# Post-clean-slate QA lifecycle — setup evidence

2026-09-24 14:59 CEST. Deployed origin: `https://j-aautomation.com`; active portal/jobs release `zip-d215671b99323d6d8c4ce34b74b4f8e0`. All writes in this phase used the existing **Finance Test** portal account in a real Chromium desktop browser. The **Project Manager Test** login was used only for read/access checks. No real client, worker, mailbox, issued invoice, outbound mail or payment was touched.

The guarded, resumable browser test is `tests/production/live-qa-lifecycle.acceptance.spec.ts`. Its private state is mode 0600 at `/tmp/jaqa-lifecycle-state.json`; the Project Manager's private browser state is mode 0600 at `/tmp/jaqa-manager-auth-state.json`. The test ran against the public deployed origin, with no local fixture reset. Final setup check: **3 passed / 0 failed** on desktop. Earlier exploratory failures are preserved in Playwright traces and explained below; they do not represent completed product steps.

## Records created through the deployed browser

| Record | Stable ID | Verified outcome |
|---|---|---|
| QA client `QA LIVE LIFECYCLE 20260924125015065` | `01a0d377-7b60-755f-91a6-df110e0d0d04` | Saved/reloaded, EUR, Europe/Madrid, `qa-lifecycle@example.test` billing contact. |
| QA project with the same marker | `01a0d377-eab6-7235-9d24-3b50f8585cd1` | Saved/reloaded as `C-0039-P-001`, cost center `QA-LIVE-LIFECYCLE`, alias `QA lifecycle`, T&M. |
| Project Manager Test assignment | `01a0d377-eab7-747c-9ced-881a989f8599` | Project create selected test user `01a0d364-e494-7333-a973-f727056e6b27`; active project-manager membership was created automatically. |

The browser submitted the project with planned end and every optional budget field blank. Read-only SQLite inspection after reload confirms `planned_end_date`, `revenue_budget_minor`, `labor_budget_minutes`, `expense_budget_minor`, `travel_budget_minor` and `po_cap_minor` are `NULL`; alias, cost center and manager ID persisted. The Finance role cannot access the project-detail Edit project button, so its absence is an authorization result, not a claim that Finance edited the project. The project remains intentionally available for the subsequent test-only people/time/expense/invoice journey.

## Deployed blockers found

1. Billing setup URL: `/j-aautomation/app/projects/01a0d377-eab6-7235-9d24-3b50f8585cd1?tab=billing&lang=en`. Finance can open the project-local setup and choose combined labor/expense billing, but Step 2 exposes only placeholder options for `Issuing legal entity` and `Labor tax profile`. `Continue` is disabled and the page explicitly says an active entity and tax profile in the project currency are required. This is expected after the clean-slate removal of business configuration. **Billing setup is not saved yet.** The test checks the blocker and will save/reload combined setup once test-only issuer/tax configuration exists. The initial attempted run produced `test-results/live-qa-lifecycle.acceptan-5ac76-r-and-expense-billing-setup-desktop/trace.zip` and screenshot.
2. Project Manager Test can sign in and open the assigned project Team tab: `GET /j-aautomation/app/projects/01a0d377-eab6-7235-9d24-3b50f8585cd1?tab=team&lang=en` returned HTTP 200. Its `Assign worker` link is absent. Direct `GET /j-aautomation/app/projects?action=assign-worker&project=01a0d377-eab6-7235-9d24-3b50f8585cd1&lang=en` returned HTTP 200 but did **not** render `form[action="?/assignWorker"]`; it showed the manager's separate project view instead. The backend may permit scoped assignment, but the deployed manager UI does not expose it. The test can be rerun with `JA_LIVE_QA_EXPECT_PM_ASSIGNMENT=1` after a UI repair to require the link and form. No worker assignments were attempted or made in this phase.

The isolated Project Manager login test initially navigated away before its asynchronous sign-in completed. Waiting for the account menu corrected the test; the final browser check passed. The duplicate manager display-name selector was also replaced with the exact test user ID; no duplicate project/client was created. All current QA IDs are in the private state file for a resumed journey.

## Additional role and device findings

The existing Finance Test account could open the deployed Billing configuration and see `New legal entity`, but submitting the clearly synthetic QA issuer form returned HTTP **403** (server correlation `7e241226-cb7f-4b48-b4ce-643cd591511c`). Read-only inspection confirmed zero new legal entities. The repository intentionally requires `owner_admin` for both legal-entity creation and invoice-number policies; the visible Finance form was a UI authorization mismatch. A local UI correction disables those owner-only actions for Finance users and hides the forms. It is **not deployed or live-verified** yet. No accountant approval timestamp was invented and no invoice number policy was created.

The separate read-only `tests/production/live-qa-mobile-readonly.acceptance.spec.ts` ran against release `zip-d215671b99323d6d8c4ce34b74b4f8e0` with the Finance Test session. **2/2 passed** in Playwright WebKit at 390-pixel iPhone and 768-pixel iPad sizes: the QA project and billing-setup region loaded, the Continue control was visible, no document overflow occurred, and reload preserved the view. This does not certify Safari on physical Apple hardware or the unconfigured downstream form steps.

Locally, `tests/e2e/project-manager-assignment.spec.ts` and `tests/e2e/billing-issuer-role-ui.spec.ts` passed **4/4** in desktop and 390-pixel phone browser projects. The former verifies a Project Manager can assign a worker visible within their authorized scope, persist it, and receive HTTP 403 for a forged outside-project assignment. The latter verifies Finance sees owner-only issuer/numbering actions disabled, with owner-access guidance, while tax-profile configuration remains available. These UI repairs have not been deployed; they do not bootstrap workers into a fresh project or grant chief authority without the real owner.

The local portal production build passed after supplying isolated test deployment identifiers and a temporary SQLite path; a first build attempt without required `JA_TENANT_ID` stopped during SvelteKit analysis and was not a code failure. The temporary build database was removed. The website build, portal typecheck and translation-catalog tests passed. Source comparison with the active release found changes in eight portal UI/translation files and no changes in the database, schemas or billing engine. No new release artifact was installed during this test-account-only check.

A final read-only count of the live SQLite database found 2 clients (IMPC and the QA client), 3 projects (the two retained BBS examples and the QA project), and **zero** legal entities, tax profiles, invoices, payments, time entries, expenses or daily reports. The QA client/project remain clearly marked for a resumed test or test-account-authorized cleanup; no accounting or mail history was created by this attempt.
