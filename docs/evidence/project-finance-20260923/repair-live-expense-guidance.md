# Deployed expense-review guidance acceptance

Date: 2026-09-24, 12:17 UTC. Origin: `https://j-aautomation.com/j-aautomation/app`. Release: `zip-d215671b99323d6d8c4ce34b74b4f8e0` (portal and site containers healthy). Authenticated ordinary owner browser session was used from a private local storage state; no credentials or cookies are included here.

The guarded Playwright test `tests/production/expense-guidance.acceptance.spec.ts` passed **1/1 on Chromium desktop** against the deployed site. It used the designated disposable QA project and worker. In a fresh run, it created a marked €1 expense with a local occurrence time of 11:25, saved the draft, submitted it, and completed operational approval. The Finance review row then displayed “Classify this expense before Finance review” and an exact “Classify expense in Finance” link. It had **no Finance review submit form/button**. Clicking the link opened Finance Commercial with the same project and expense IDs, focused the expense-classification section, and exposed the classification form for that expense. The form remained visible after page reload.

Read-only database corroboration for the fresh browser run: expense `01a0d358-e83a-76ba-be49-e5fc40d72242` belongs to QA project `01a0cfae-9005-76a9-871a-8cf4edf877dd` and QA worker `01a0d080-0b0c-779c-88f3-6d2458c4616d`; amount is 100 EUR minor units, occurrence time `11:25`, operational state `approved`, commercial classification state `unclassified`. This confirms the UI did not accidentally complete Finance review or classify the expense. No invoice, payment, mail or settlement action was performed.

An initial test attempt asserted the list before the asynchronous save finished; the row was in fact persisted. The test now waits for the save surface to close and reloads before finding the new row. A resume run on that original QA expense also passed **1/1**, then the fresh end-to-end run above passed **1/1**. Both marked QA expenses are disposable in the approved clean-slate cutover. This check covers the repaired owner Finance guidance path; it does not claim a separate worker-role or physical-device run for this specific scenario.
