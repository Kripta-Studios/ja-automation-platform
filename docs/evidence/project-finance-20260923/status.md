# Project/workforce/finance rollout status — 2026-09-23

This is a progress ledger, not a release acceptance certificate.

## Test-account-only post-clean-slate check — 2026-09-24

An existing Finance Test account created and reloaded a clearly marked QA client/project on the deployed `zip-d215671b99323d6d8c4ce34b74b4f8e0` release; optional budgets and planned end persisted as null, while cost center, alias and Project Manager Test assignment persisted. The live desktop setup test passed 3/3, and read-only WebKit checks of the QA project billing setup passed 2/2 at iPhone/iPad sizes. Full live people → time → expenses → invoice remains **BLOCKED**: a fresh project has no owner-bootstrapped workers or chief grant; the deployed PM assignment form is absent; billing setup lacks a legal entity/tax profile; legal-entity and invoice-number policy creation require the canonical owner, and no accountant-approved test policy exists. A Finance Test attempt to save a synthetic legal entity returned 403 without creating one. No live invoice was issued, emailed, marked sent, paid or reversed. Physical Apple Safari was not tested. See [live-qa-lifecycle.md](./live-qa-lifecycle.md).

The scoped PM assignment and owner-only billing-control UI repairs passed local desktop/phone browser tests 4/4 but are **not deployed**. The corrected isolated 32-step browser journey passed steps 1–29, including report sign-off, invoice issuance, partial payment and Accounting Pack creation; steps 30–32 lack current external operator/Caddy evidence, so the suite exits 1. Separate isolated invoice-email browser coverage proves queueing only. See [local-32-step-browser-20260924.md](./local-32-step-browser-20260924.md) and [isolated-invoice-20260924.md](./isolated-invoice-20260924.md).

## Current deployed result — 2026-09-24, 14:25 CEST

- The active portal/jobs/site release is `zip-d215671b99323d6d8c4ce34b74b4f8e0` (source ZIP SHA-256 `d215671b99323d6d8c4ce34b74b4f8e09c860a7bad1a662a542a8350362e6365`), with database migration **60**. Portal/site health, public API health and jobs startup passed. The repair release's live billing readiness/browser test passed **3/3** on desktop and iPhone/iPad WebKit: an invalid draft retains its Billing tab, inputs, explanation and setup link. The expense-guidance journey passed **2/2**: an approved but unclassified QA expense blocks Finance review and links to the exact classification form. See `repair-live-billing.md` and `repair-live-expense-guidance.md`.
- Earlier on the same feature series, actual deployed owner/chief journeys proved mixed per-person terms, team hours, linked/shared expenses, report navigation, calculation explanations, and combined/separate draft invoices with opening PDF files; the one-invoice example was €110 labor + €10 expense = €120, and the two-invoice mode produced €110 and €10 drafts. No QA invoice was issued, sent or paid. See `live-crew-journey.md`, `live-invoice-modes.md`, and the detailed acceptance entries below. The d215 release changed only the identified readiness/approval guidance and locale surfaces; the affected flows were retested above.
- At 14:21 CEST, the authorized production clean-slate cutover completed using a fresh cold copy, exact archive hashes, an idempotent cleanup rehearsal and a paired DB/artifact promotion with both SQLite sidecars moved out. Clients **27→1** (IMPC), projects **44→2** (BBS Mexico/Junkers DFW), invoices **20→0**, time entries **128→0**, expenses **64→0**; all business artifacts were archived (63 files) and zero remain active. Users/accounts **118/118** and mail identities **99** were preserved exactly at cutover. The original database and artifacts are in `/home/kripta/ja-clean-slate-cutover-20260924T122152Z/`; its pre-clean database SHA-256 is `318bbf0afc32eee36ee3d5131a3d6dfa688d57877c5eca5a38f053cf95aac4cf`. SQLite integrity and foreign keys passed. See `clean-slate-live-integrity.md`.
- Post-cutover public-origin browser checks passed **12/12** owner create/reload/delete tests across desktop, 360/390 phone, 768 tablet and WebKit iPhone/iPad. The app returned to exactly one client and two projects with no QA residue. Worker/finance/chief read boundaries passed **9/9** on desktop/WebKit. A read-only tour of empty Time, Expenses, Reports, Finance and Billing pages passed **3/3** desktop/WebKit. See `clean-slate-live-browser.md`, `clean-slate-live-roles.md`, `clean-slate-live-views.md`. The automatic ZIP watcher was held during acceptance; Stalwart remained active without restart.
- Three new portal-only Finance Administrator, Project Manager and Auditor test accounts were subsequently created in the owner browser with the requested local-part password convention. Browser sign-in and allowed/forbidden route checks passed; existing eight worker test accounts use that convention. The private, mode-600, git-ignored test-account manual has their credentials and correct post-cleanup grant instructions. Users/accounts are now **121/121** by intended addition; mail identities **99** and mailbox commands **8** remain byte-for-byte identical to the cold backup, with no portal outbox mail queued. The ZIP path/timer were restored after acceptance. See `role-test-accounts-live.md`.
- Final focused invoice lifecycle, invoice invariant and reporting-artifact tests passed **11/11**. The first run had one test-only PDF text-extraction assertion fail because the approval-status column separated words in an operational description; the fixture now checks unique content independently of extracted column order. No product code or deployed artifact changed in this follow-up.
- Limits: Native Safari on physical devices was not run; WebKit at iPhone/iPad sizes was. Live customer invoice **issue/send/payment** was intentionally not used as a QA action; draft and PDF behavior was verified before the clean-slate cutover. Newly clean projects will require commercial/issuer setup and approved records before invoices are ready. The real canonical owner remains the only owner account, and supplier/chief special access requires a new scoped business grant.

## Prior deployed release and acceptance — 2026-09-24, 13:18 CEST onward

- Release source ZIP SHA-256 `b0276b4dbd5583c3315794d91cbca213d0d84a3032b06c50190f93ea828b1c6a` deployed as `zip-b0276b4dbd5583c3315794d91cbca213`. Portal and site were healthy, jobs ran, public login/site checks passed, and SQLite migrated to version **60** with `integrity_check=ok` and zero foreign-key violations. The deployer's pre-switch online backup is `2026-09-24T111757078Z-9e531000-a0ad-4112-85f6-a84dc5423ede`. The mail service was not changed.
- On the public origin, desktop acceptance had 11 passes, one configured skip and one stale test locator after “Skill” was renamed “Expertise”; correcting only that locator yielded a passing rerun. The five Chromium phone/tablet/touch profiles then passed **60** checks with five configured skips. WebKit iPhone/iPad billing setup passed **2/2**; WebKit project/finance/navigation passed **12** with two configured skips. These exercises created and edited real QA projects, saved/reloaded combined and separate billing configuration, checked the mixed-rate €3,640 draft, source links, and role visibility.
- The deployed calculation explanation passed **6/6** desktop/WebKit iPhone/iPad owner/worker checks: owner source breakdown survived reload; worker received 403 for private finance terms. The chief browser journey passed **2/2** desktop checks and **2/2** WebKit touch-layout checks. It saved distinct and shared per-worker duration entries, a timed linked expense, and one €3 shared receipt allocated €2/€1 without multiplying the expense; read-only SQLite confirmed worker/actor IDs, amounts and links. See `live-crew-journey.md`.
- A fresh version-60 production-data copy passed clean-slate rehearsal and idempotent rerun, then an isolated authenticated owner browser read: one IMPC client, two BBS example projects, empty operational time/expense/report/invoice screens, intact 118 user/account rows and 99 mailbox identities, integrity and foreign keys clean. See `migration60-clean-slate-rehearsal-20260924.md` and `clean-copy-app-browser.md`. **The production cleanup has not yet been performed.**
- Actual combined/separate draft generation passed in marked live QA projects: one combined €120 draft has €110 labor and €10 expense sections; separate mode has €110 and €10 drafts. Details survived reload, PDFs opened, each source was billed once, and no invoice was issued/sent/paid. See `live-invoice-modes.md`. A further fresh separate-mode run saved its setup but stopped at a transient `net::ERR_ABORTED` during Approvals; the completed resumed-project journey establishes the draft result.
- The same journey exposed generic 409 errors when Finance review preceded expense classification and when classification preceded canonical project issuing-authority assignment, plus a project detail cadence rejection that hid its existing explanation. Actionable approval/classification links and invoice-sheet preservation are implemented in the workspace and await a follow-up release and affected live retest. The validity of the underlying finance rules is unchanged.
- Docker builder-cache pruning after this release reclaimed **6.964 GB** without removing the running or rollback images. Disk free space was about 16 GB afterward.

## Latest deployed verification — 2026-09-24

- The current portal/jobs/site release is `zip-d444cae7d8a9b6c5d4d6309b72177837` (full source checksum `d444cae7d8a9b6c5d4d6309b7217783751474c75104d02a515a92fc12b7cd2a9`). Portal and site containers are healthy; the jobs container is running. Database migration 0055 was rehearsed on an isolated copy of the production backup with `integrity_check=ok` and zero foreign-key violations before deployment. The deploy backup is `2026-09-24T081607305Z-33ea60a4-062e-46b5-8e55-3453451b7304`. Mail services were not changed.
- The authenticated production Playwright suite against `https://j-aautomation.com` passed **37 checks**, with **3 configured desktop-only duplicates skipped**, in 1.7 minutes. It covered 360/390 phone, 768 tablet and 1440 desktop: create/reload/delete a minimal project with optional budgets/end date blank, project/time/report links, Finance commercial summary, the eight-line mixed-rate draft invoice, owner alias edit/reload/restore, worker and finance boundaries, chief crew and delegated expense entry. The chief expense picker correction was independently retested **4/4** at those widths. Browser outcomes were asserted after reload, not inferred from local unit tests.
- A separate read-only touch-emulated Chromium check of the current deployed release passed **2/2** at 390-px iPhone and 768-px iPad profiles (`isMobile`, touch and device scale enabled). It exercised navigation drawer, visible/touch-sized project creation fields and project detail overflow. It does not substitute for Safari-device testing or the unreleased billing setup journey.
- A designated QA project has eight portal-only QA workers with individually saved commercial terms: seven billed at €55/hour and paid €30/hour, one billed at €70/hour and paid €45/hour. Eight approved 8-hour source rows produced an eight-line **draft** labor invoice for €3,640, 64 hours. The browser displayed the lines at all four widths and downloaded a PDF whose extracted content matched the source lines and total. See `live-mixed-rate-20260924.md`. No live invoice was issued, sent or paid.
- A deployed 390-px worker browser saved 0.5 hours and a €1.20 shift-linked expense in one atomic request after first rejecting invalid €0 input; the persisted rows share exactly one idempotency request. The chief browser saved a separate €1.25 delegated shift-linked draft expense with an occurrence time, worker and recorded-by actor, and no commercial pay exposure.
- In a separate deployed-browser check, the owner created QA installation expertise, assigned proficiency 4 to QA Worker 1, reloaded Planning, and filtered the project assignment picker to that worker. The record and worker-expertise relation were verified in SQLite; the browser check passed **4/4** at 360/390/768/1440 widths. The existing Planning screen still says “Skill” in the current release; a consistent Expertise label is being implemented locally.
- A fresh read-only cleanup audit still identifies **18 protected roots, 36 unclassified roots, 99 mailbox-backed users and 30 unresolved dependency relations**. It does not establish a safe deletion set, so requested mock-data removal remains incomplete; no unclassified business record or mailbox was deleted.
- A private 0600 classification sheet at `docs/manuals/Business_Data_Classification.private.md` now lists the 36 unclear client/project IDs and names for provenance review. The cleanup applier remains rehearsal-only and has no positively verified deletion candidates.
- The owner subsequently clarified the desired final state: remove non-BBS/IMPC clients, projects, reports and related test business data, while preserving **all users and mailbox identities** and the BBS/IMPC examples. This supersedes the earlier request to classify ambiguous rows individually. Cleanup is scheduled after feature acceptance so live QA records can be removed too; backup, dependency closure, issued-history treatment and an isolated rehearsal remain prerequisites. No production cleanup has run yet.
- A new deployed-browser clean-slate smoke test exercised client creation, project creation, reload, project deletion and client deletion on the current release. Its first run timed out while its cleanup tried to click a collapsed client panel; the remaining QA client was removed through the owner browser, and a corrected run passed **1/1** on desktop. A read-only database query confirmed zero clients/projects with that QA marker remain. The separate post-cutover retention assertion has not run because production cleanup has not happened.
- The latest isolated clean-slate rehearsal used a fresh migration-55 online production copy captured 2026-09-24 09:26 UTC. Clients **27→1** (IMPC), projects **27→2** (BBS Mexico/Junkers DFW), users/accounts **118→118**, mailbox identities **99→99**, invoices **17→0**, daily reports **43→0**, payments **6→0**, DEMO/QA legal and tax settings **→0**, test suppliers and supplier profiles **→0**, and numbering scopes **11→1** (global counter only) in the active copy. The two supplier-role test users remain portal accounts but need assignment to a newly created supplier before supplier-specific testing. The original copied database (SHA-256 `4954a111332e379e3e9d8d382df8ce5dfd0b4212dafe3a5f03bbdd79c81fdd6d`) and 62 artifacts were archived with hashes. The exact post-clean logical manifest SHA-256 is `0b102cf08603f5a43d98542313207f5aa223cfccb93b58ae8ca2c6206255d90f`. The active copy and archive pass `integrity_check=ok` and zero FK failures; protected-row hashes, trigger restoration, rollback injection, and idempotent rerun passed. Focused tests passed **14/14**. This is **not** a production cutover; second independent review and fresh cutover backup/switch/browser/mail checks remain. See `clean-slate-rehearsal-20260924.md`.
- A newer compatibility rehearsal migrated another online production copy **55→58**, then ran the revised cleanup with exact retained-artifact checks. It passed application and idempotent rerun: one IMPC client, the two BBS projects, 118 users/accounts, 99 mailbox identities, zero active invoices/reports/suppliers, all 62 artifacts archived, `integrity_check=ok`, zero FK failures and 418 retained triggers. Its original migrated-copy SHA-256 is `36f4dc5d974d1bf8f009f7c22c6c10d7401bd3892c6a279a32c7e2656d3db077`. Focused script tests passed **15/15**. Production remains unchanged; final independent review and cutover validation remain.
- A subsequent review found the v5 rerun could accept a missing retained artifact. The local cleanup code now journals the exact survivor artifact path/hash manifest, requires every survivor before recovery, and verifies the exact active file set after deletion. A regression deleting retained `bbs.pdf` fails as expected; focused tests pass **15/15** and targeted TypeScript checking passes. The corrected code also passed a fresh migration-58 rehearsal on copied production data; independent final review, restored-app browser/mail checks and controlled cutover remain pending.
- Docker builder cache pruning reclaimed **6.961 GB** after the release. Four superseded local source archives were removed while the current and immediate prior release archives and Docker rollback images were retained. Disk free space rose from 4.9 GB to more than 11 GB before archive removal.
- Remaining product gaps under active work: deploy and live-test shared-receipt allocation, budget edit units, people-first creation, project-centric billing templates, and combined/separate time-and-expenses invoicing. Project-level expertise requirements and per-person budget forecasts need further implementation. Issued invoice/payment lifecycles and the full cleanup need separate business-data evidence. Earlier sections below are historical observations for previous artifacts and must not be read as the current release verdict.

## Current unreleased implementation (still local)

- The project Billing setup defaults to one invoice with labor and expense sections and can save two separate invoices, with a reusable template and project-local review. After initial fixes, focused integration/translation checks passed **22/22** and its browser checks passed **6/6** at 360/390/768/1440 widths. Independent review still marked it **FIX-FIRST**: Step 3 cannot edit each person's commercial terms within the project, and conditional cadence/template errors can point to hidden fields. Those issues are being corrected; deployed acceptance remains pending.
- Approved invoice recovery was added locally and initially passed focused tests **5/5** and a desktop browser flow **1/1**. Independent finance review marked it **FIX-FIRST** because recalculation deleted the old approved financial snapshot and archiving configuration could strand an approval. An immutable supersession record and archive safeguards are being implemented; the initial tests are not release evidence for these invariants.
- A finance-only project period explanation now groups canonical operational time, expenses and milestones and distinguishes pending, approved operational, approved-unbilled WIP, invoiced and paid totals. After two review rounds, its focused integration checks passed **6/6**, DB/portal typechecks passed, and local browser checks passed at 360/390/768/1440 widths with numerical assertions. Immutable selected-rule IDs/versions are not yet in the canonical finance projection, so the page marks that provenance unavailable and does not claim invoice readiness. It is unreleased.
- Planning assignment forms now show planned hours and submit integral minutes. A focused unit suite passed **3/3**; the planning calendar browser suite passed **12/13** on the first run, with one unrelated multiworker test needing its interval-mode step and expected 403 denial updated. That targeted multiworker test then passed **1/1** on rerun. The revised production acceptance suite includes a guarded four-width browser journey for both billing modes; it has been listed but not run against a new release.
- A people-first project creation step now selects active workers by expertise and saves project plus assignments transactionally; cost center remains required. Focused integration, i18n and four-width browser checks passed locally. Commercial terms are configured in a later project step.
- The project budget editor now presents currency units and hours rather than minor units and minutes; unit, integration and four-width browser checks passed locally.
- Shared-receipt allocation now records one financial source with per-worker shift allocations whose amounts sum to the receipt; migration, integration and four-width browser checks passed locally. Independent final review found no blocking defect. These changes are not deployed.
- Project assignment expertise filtering and persistence passed desktop, 360/390 phone, and 768 tablet browser checks (4/4).
- Crew-chief grants, individual/shared per-worker hours, actor attribution, scoped chief detail, and responsive controls passed local browser checks at the same four widths (4/4). The database prevents forged recorder rows and rewriting historical grant scope.
- An expense entered from a time record persists its occurrence time and shift link; worker browser checks passed at four widths (4/4). Chief-entered expenses also passed at four widths (4/4), with delegated scope and private reimbursement data withheld.
- The Finance browser saved explicit per-person customer, compensation, and cost rule references and reloaded them (1/1). The issuing-authority browser created a canonical revision, assigned it to a project, and reloaded the persisted history (1/1).
- A draft PDF preview now uses the canonical issuer effective for the invoice period and refuses an unconfigured/mismatched issuer; its browser test passed (1/1). This code has not yet been deployed, so the prior live release remains the only production result.
- Independent review identified implicit global pay/cost fallback, ignored direct assignment references, percentage pay silently becoming zero without a client rate, mutable crew-grant provenance, and a mutable legacy issuer on draft PDFs. The current local code addresses these; final combined review and deployed acceptance are pending.
- A separate production Playwright configuration now avoids fixture-resetting local setup. Its current suite covers project links, owner alias round-trip, and role read boundaries; the complete live financial journey remains to be run and evidenced.
- Expense-policy migration 0054 and the numerical eight-worker invoice/expense reconciliation now pass focused tests. New expenses require effective assignment policies; Finance displays the selected worker reimbursement and customer recovery, permits a reasoned one-expense override, and rejects unsupported foreign-currency reimbursement at the server boundary. The release build and deployed browser acceptance are still in progress or NOT RUN.
- The combined local desktop browser journey passed 10/10 checks covering minimal project creation/validation, expertise assignment, crew time and expense entry, time-linked expense, person-specific commercial and expense terms, canonical issuing authority, and a draft PDF with its labor line. Expense-policy focused integration/migration/i18n tests passed 75/75, and full workspace typecheck passed. A representative online backup of the current production database migrated on an isolated copy through version 54 with `integrity_check=ok` and zero foreign-key violations.
- An independent final finance/security review found no remaining code blocker after corrections to policy-aware classification, FX validation, historical issuer display, and the policy-resolution link. These are local findings, not deployed acceptance.

## Deployed browser baseline

- The 2026-09-24 `zip-c3d2387d16ec7d539668f370a1bd08d1` release deployed migrations through 0054, with a pre-release backup and no mail service changes. Its first authenticated production acceptance run was **8 passed, 10 failed, 6 skipped** across desktop/tablet/phone; Finance returned HTTP 500 because an existing demo project has overlapping active assignments for one worker. A real browser also reproduced HTTP 400 on minimal QA project creation: selecting the EUR client left the form's currency at USD. These are deployed **FAIL** results, not local-test passes.
- A follow-up source fix makes ambiguous commercial terms a Finance configuration issue rather than an exception during both actual-time pricing and forecast. On an isolated online copy of the production database, the portfolio now returns 27 projects, 4 incomplete, including 1 ambiguous assignment; focused V3 tests and typecheck pass. The project form now derives currency/timezone from the selected client and shows a field error for an explicit currency mismatch. This follow-up is **local only** until a new release and browser retest.
- Release `zip-6d3be41235578cbdc68f3da4ad19bfc0` deployed that follow-up. The authenticated production acceptance suite passed **18/18 run** at 360/390/768/1440 widths (6 desktop-only cases skipped at other widths): create/delete minimal EUR-client project, Finance views, project/time/report links, alias round-trip, and worker/finance access. This supersedes the prior release's failed result for those checks.
- On this deployed release, the owner created and assigned a **test-only** EUR canonical issuer revision to the designated QA project. Finance saved a €30/hour internal cost for Worker Test and a worker-paid parking policy with reimbursement and customer recovery at cost, then classified a legacy €4.50 expense. Worker Test recorded a new €2.00 parking expense at 14:25, linked it to the 7.5-hour source time entry, submitted it; owner approval, Finance classification and Finance approval all saved and survived reload. The new policy preview showed €2.00 worker reimbursement and €2.00 customer recovery. This is QA data only.
- The live expense-stream wizard first blocked a draft because the classified expenses lacked the separate Finance review; after the authorized Finance reviewer completed it, the owner saved a draft expense invoice for 2026-09-23. The database contains two source lines (€4.50 and €2.00), subtotal/total €6.50 and no invoice number. The authenticated browser detail showed both lines, and its downloaded A4 draft PDF (260,196 bytes, SHA-256 `d08e02352d74ce2b47e4c48168127bd3671b99a6bd346dc72b894d7bd53082fb`) contained both amounts and the €6.50 total. No issue/send/payment occurred.
- Worker Test created a same-day QA daily report; the owner approved it. The normal report job produced an English PDF (245,424 bytes, SHA-256 `5e20afac15495562b6c6de4161bc1864ec8416ed537091e506d94a72dae1ca75`) with the saved shift/task text, downloaded in an authenticated browser. The time detail did not link this report on the current live release; a scoped, authorization-checked link is implemented locally and awaits deployment/retest.
- Production cleanup audit reports 27 clients, 27 projects, 18 protected roots and 36 unclassified roots; no root has positively attested fixture provenance. It finds 99 mailbox-linked users and 30 incomplete dependency relations. Therefore mock/test cleanup remains **NOT DONE**; neither mailbox data nor ambiguous business records were deleted.
- For live crew acceptance, the owner assigned the existing non-mailbox Supplier Coordinator Test and External Technician Test to the designated QA project, then granted the coordinator dated chief authority over External Technician Test and Worker Test. A browser sign-in as that chief succeeded, but `GET /app/crew?project=...&date=2026-09-24` returned **403** because the crew repository used the UTC calendar date (September 23) while the project timezone Europe/Madrid was already on September 24. The repository now evaluates grant/member current dates in the project timezone, with a boundary regression. This correction is **local only** until the next release and live chief retest.
- Release `zip-2d79055b8f463e5fc9795076cc5f8850` deployed the local-date and time-to-report changes. The live time detail rendered both matching daily reports (one draft, one approved), proving the link and authorization selection worked; the initial new browser test used a nonunique link label and is being corrected to the stable report ID. The chief route still returned **403** because the pre-existing supplier-account route allowlist rejected `/crew` before the repository ran. The route allowlist now includes exact crew and scoped expense-option paths locally, with negative security cases retained; another release and live chief test are required. After the third deployment, `docker builder prune -af` reclaimed 12.3 GB without removing rollback images or touching mail.

- Public production origin: `https://j-aautomation.com/j-aautomation/app/projects`.
- Existing Finance Administrator account signed in through the actual browser.
- A valid, explicitly marked QA client with billing contact and address saved (HTTP 200; SQLite row present). Its stable ID and exact name are in `live-valid-client-project-attempt.json`.
- On the former `zip-97502e4a6676a5a7c91435e42522c3b3` release, creating a project for that client with required name/cost center and optional budgets/planned end empty returned HTTP 400, showed only “Check project fields,” and inserted no project.
- Release `zip-4cbf52f162a73ccaadcea8d9d8405e7b` was deployed. The same authenticated browser journey now saves a project (HTTP 200); the required cost center persists, and optional budgets/planned end remain null after reload. See `live-released-project-create.json`.
- The owner changed that project's alias and cost center through the browser (HTTP 200, persisted), assigned the existing test worker (HTTP 200, persisted), and the worker recorded 7.5 hours (HTTP 200, 450 minutes, no invented interval). See the corresponding `live-released-*.json` files.
- The worker recorded a $4.50 QA parking expense from the project-filtered Expenses screen (HTTP 200; record visible as a draft). See `live-released-expense-attempt.json`.
- That first QA expense used USD on a EUR project and caused an explicit missing-FX finance warning. The test draft was deleted through the worker UI, recreated in EUR, and the warning cleared after reload. This was a QA data correction, not an FX-engine fix.
- The owner created a clearly marked test-only EUR legal entity and zero-tax profile and saved separate manual labor and expense billing streams through the browser (HTTP 200 each). Both streams appear as active on the project after reload. Automatic draft generation is off. The project has no reviewed canonical EUR issuing-authority revision.
- The worker saved a project-linked daily report in the deployed browser (HTTP 200), and the project detail showed one report after reload. The worker submitted the QA time and expense; the owner operationally approved both and recorded the time as billable in Finance review. These are QA records, not customer acceptance.
- The owner saved a project/worker client rate of €55/hour. A first compensation save with an effective date before the worker's assignment failed HTTP 400 with only a generic error. Setting the date to the assignment start succeeded. The UI still needs a specific effective-date validation message. Internal loaded cost remains unconfigured.
- A real QA labor invoice draft was created from the approved 7.5 hours: the browser preview and register show €412.50 (7.5 × €55), with no issue/send action performed. The first release allowed a localized PDF job for this draft; that artifact said **“No invoice lines”** despite the total. This was a confirmed production **FAIL**. Release `zip-edc82bc9c8f4b2d7ea53c75c0e0b1861` now rejects localized PDF variants for unissued invoices and provides a dedicated read-only draft PDF preview. An authenticated browser downloaded its 259,986-byte PDF and extracted the 7.50-hour line at €55.00 and €412.50 total; the “No invoice lines” text is absent. The draft PDF repair is **PASS** on the deployed release. The historical bad variant remains an invalid prior artifact, and issued-invoice generation is not yet proven by this check.
- Classifying the EUR QA expense as reimbursable at cost failed HTTP 409 because the only reviewed canonical issuing-authority revision is the USD demo entity. The separate test-only EUR legal entity is not a canonical revision. The app needs a safe UI/workflow for that authority before EUR expense classification and full invoice lifecycle can pass.
- Project → Time preserves the project filter, and **Log time** now preselects that project. This was checked in an authenticated worker browser on `zip-edc82bc9c8f4b2d7ea53c75c0e0b1861`; the selected ID matched the QA project. The project-to-time prefill repair is **PASS** on the deployed release.
- No test invoice was issued or sent. No mail service was changed.

## Verified locally

- New project with all optional budgets/planned end blank: browser save, persisted nulls, detail reload.
- Invalid project name: specific field error, form remains open, values retained.
- Duration-only time: 7.5 hours persists as 450 actual minutes without fabricated clock times.
- Supplier coordinator: shared interval journey, different hours per technician in one atomic/idempotent batch, and shared duration journey. Local browser tests assert stored minutes.
- Supplier integration test covers transaction rollback, retry, authorization, and distinct per-person minutes.
- Portal typecheck, focused unit/integration/security/invariant tests, Docker release build and relevant local browser tests passed. Full feature delivery and deployed acceptance remain pending.

## Test account handling

**Publication update, 2026-09-24:** At the owner's explicit request, the test-account manual and business-data classification sheet are now included in the public GitHub repository. The handling described below records the earlier state.

- The six existing non-owner test-role credentials in the server credential file were verified against the live password hashes. The private manual is `docs/manuals/Portal_Test_Accounts.private.md`, owned by `kripta`, mode `0600`, excluded from Git and Docker context.
- Those existing passwords differ from the mailbox local parts. No password or mail identity was changed. The unique canonical owner is not represented as a test account.

## Required next evidence

Continue expense classification/canonical EUR issuing authority, calculation reconciliation, billing streams, issued artifact rehearsal, report navigation, role visibility and cleanup checks. Implement and test the remaining plan packages; an item without browser and persistence evidence remains NOT RUN, not PASS.
